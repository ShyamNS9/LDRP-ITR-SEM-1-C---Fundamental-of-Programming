               /*.................GREATER SMALLER 1...................*/
#include<stdio.h>
#include<conio.h>

int main()
{
   int a,b,c;
   printf("Give two nmubers to find max among them: ");
   printf("\nNumber1 = ");
   scanf("%d",&a);
   printf("\nNumber2 = ");
   scanf("%d",&b);
   if (a>0,b>0,a>b)
   {
     printf("\n%d or num1 is the greater number",a);
   }
   else
   {
     printf("\n%d or num2 is the greater number",b);
   }
   if (a>0,b>0,a<b)
   {
     printf("\n%d or num1 is the smaller number",a);
   }
   else
   {
     printf("\n%d or num2 is the smaller number",b);
   }
}